{
  "name": "@gsmflow/adapters-base",
  "version": "1.0.0",
  "description": "Base runtime adapter interfaces for GSMFlow",
  "main": "src/runtime.ts",
  "types": "src/runtime.ts",
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
