{
  "name": "@gsmflow/adapters-deno",
  "version": "1.0.0",
  "description": "Deno Deploy adapter for GSMFlow",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@gsmflow/adapters-base": "workspace:*",
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
